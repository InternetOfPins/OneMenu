/**
 * @file menu.h
 * @brief Convenience forward to the AM4-compat layer for consumers that
 *        depend on OneMenu directly (not through the ArduinoMenu/AM5 wrapper)
 *        and want the familiar `#include <menu.h>` AM4 spelling.
 * @see oneMenu/compat/am4.h — the canonical, uniquely-named implementation.
 */
#pragma once
#include <oneMenu/compat/am4.h>
